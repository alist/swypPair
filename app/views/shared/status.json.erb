{"swypToken": <%= @swyp.swypToken %>, "status": "<%= @swyp.status %>",  "peer": 
<% if @swyp_peer != nil %> {
	"id" : <%= "#{@swyp_peer.swypToken.to_s}" %>,
	"port" : <%= "#{@swyp_peer.port.to_s}" %>,
	"ip" : "<%= "#{@swyp_peer.address}" %>",
	"pubKey" : "<%= "#{@swyp_peer.publicKey}" %>"	
}<% else %> null <% end %>}