{"swypToken": <%= @swyp.id %>, "status": <%= @swyp.status %>,  "peer": 
<% if @swyp_peer %> {
	"id" : <%= "#{@swyp_peer.id.to_s}" %>,
	"port" : <%= "#{@swyp_peer.port.to_s}" %>,
	"ip" : "<%= "#{@swyp_peer.address}" %>",
	"pubKey" : "<%= "#{@swyp_peer.publicKey}" %>"	
}<% else %> null <% end %>}